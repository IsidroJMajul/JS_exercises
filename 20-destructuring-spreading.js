// DESESTRUCTURACION
// permite extraer valores de arrays u objetos y asignarlos a variables


// Sintaxis arrays

// Sintaxis arrays con valores predeterminados
// En caso que no tenga valores, para imprimir con un valor por defecto

// Ignorar elementos del array

// Intercambiar valores

// Sintaxis objects
// Hay que usar {}
// Los valores de la desestructuracion, respeta las keywords insertas en el objeto. No puede cambiarse el nombre, caso contrario tendremos un elemento "undefined"
// el orden no altera la propiedad, porque busca las keywords

// Sintaxis objects con valores predeterminados

// Sintaxis objects con nuevos nombres de variables

// Objetos anidados y en diferentes niveles

/*  */

// PROPAGACION
// operador (...)

// Sintaxis arrays

// Copia de arrays

// Combinacion de arrays